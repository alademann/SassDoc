<header role="banner" class="wdesk-docs-header">
    <div class="container">
        <h1>{{ project_title }}{% if version %}<small> - {{ version }}{% endif %}</small></h1>
        <p class="lead">{{ item_count }} Sass entitie{% if item_count > 1 %}s{% endif %} documented</p>
    </div>
</header>